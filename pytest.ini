# pytest.ini

[pytest]
testpaths = tests
pythonpath = . src
addopts = -m "not integration and not full_dataset"
markers =
    integration: tests that require generated local artifacts
    full_dataset: tests that require the full dataset to run
